import { a,b,c } from "./a" // 批量导入
import { P } from "./a" // 导入接口
import { f as F } from "./a" // 导入时候起别名
import * as All from "./a" // 导入所有模块中所有成员，绑定在All 上
import myFunction from "./a" // 不加{} 导入默认